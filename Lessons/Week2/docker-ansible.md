#Librerías a revisar en donde se encuentra el docker
## docker-py
* Instalarla con root (sudo su)

---

#ansible-playbook -s redis.yml -k --ask-become-pass
#comando para ejecutar el playbook y hacerlo con sudo el ansible
